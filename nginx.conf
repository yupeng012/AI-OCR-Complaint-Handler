server {
    listen 80;
    server_name yupeng-ai.cn www.yupeng-ai.cn;

    # Redirect HTTP to HTTPS (after SSL setup)
    # Uncomment after SSL certificate installation
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://localhost:8501;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        
        # Buffer sizes
        proxy_buffer_size 4k;
        proxy_buffers 4 32k;
        proxy_busy_buffers_size 64k;
    }

    # Static files caching
    location /_stcore/static/ {
        proxy_pass http://localhost:8501;
        proxy_cache_valid 200 1d;
        add_header Cache-Control "public, max-age=86400";
    }

    # Error pages
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}

# HTTPS configuration (after SSL setup)
# server {
#     listen 443 ssl http2;
#     server_name yupeng-ai.cn www.yupeng-ai.cn;
# 
#     ssl_certificate /etc/letsencrypt/live/yupeng-ai.cn/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/yupeng-ai.cn/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers high;
#     ssl_prefer_server_ciphers on;
# 
#     location / {
#         proxy_pass http://localhost:8501;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
